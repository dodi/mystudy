<%= form_for(@marketing) do |f| %>
  <% if @marketing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marketing.errors.count, "error") %> prohibited this marketing from being saved:</h2>

      <ul>
      <% @marketing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :jumlahpeserta %><br>
    <%= f.number_field :jumlahpeserta %>
  </div>
  <div class="field">
    <%= f.label :keterangan %><br>
    <%= f.text_area :keterangan %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
